# DeepL API Configuration
# Get your API key from: https://www.deepl.com/pro-api
DEEPL_API_KEY=your_deepl_api_key_here

# Google Cloud Translation API Configuration
# Path to your service account JSON file (relative or absolute)
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_APPLICATION_CREDENTIALS=./credentials/your-service-account.json

# Optional: Google Cloud Project ID (auto-detected from credentials JSON if not set)
# Required for v3 API
GOOGLE_CLOUD_PROJECT=your-project-id

# Translation Service Selection (optional)
# Options: 'deepl', 'google', or 'auto' (default: auto-detects based on available credentials)
TRANSLATION_SERVICE=auto

# Google Cloud Translation API Version (optional)
# Options: 'v2' (Basic) or 'v3' (Advanced). Default: v3
GOOGLE_TRANSLATE_API_VERSION=v3

# Proxy Configuration (optional, if your IP is blocked)
# Format: http://user:pass@host:port or http://host:port
# DEEPL_PROXY=http://proxy.example.com:8080
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080

# Confluence API Configuration
# Base URL of your Confluence DC instance
CONFLUENCE_URL=https://yourdomain.com
# Bearer token for API authentication
CONFLUENCE_BEARER_TOKEN=your_bearer_token_here

# Target Language (optional, defaults to Russian)
# Examples: ru_RU, de_DE, fr_FR, es_ES, it_IT, pt_BR, ja_JP, ko_KR, zh_CN, zh_TW, pl_PL, nl_NL, sv_SE, fi_FI, uk_UA
TARGET_LANGUAGE=ru_RU
